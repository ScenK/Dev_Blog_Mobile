import serverConfig from '../configs/server';
import _ from 'lodash';

var TestingHistoryService = {
    name: 'testinghistory',
    read: function (req, resource, params, config, callback) {
    }
}


module.exports = TestingHistoryService;
