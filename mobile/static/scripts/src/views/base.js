define(function (require) {
  var $        = require('jquery'),
      Backbone = require('backbone');

  var Base = Backbone.View.extend({
    init: function () {

    }
    
  });

  return Base;
});
